#!/bin/sh

set -u

dist=$1
arch=$2
name=$3
vers=$4

while true; do
    apt-get update
    if [ $? -ne 0 ]; then
	sleep 30
	continue
    fi
    apt-get -q --download-only source ${name}=${vers} && break
    sleep 30
done
