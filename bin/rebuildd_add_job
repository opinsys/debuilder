#!/bin/sh

set -u

status=$1
suite=$2
package=$3
version=$4
srcpath=$5

case $status in
    accepted)
	for architecture in i386 amd64; do
	    echo "$package $version 1 $suite $architecture" | rebuildd-job add
	done
	;;
    *)
	;;
esac
