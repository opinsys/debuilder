#!/bin/sh

set -u

dist=$1
arch=$2
name=$3
vers=$4

pbuilder --build --basetgz /var/cache/pbuilder/${dist}-${arch}.tgz \
    --buildresult /var/cache/pbuilder/result_${dist}_${arch} \
    ${name}_${vers}.dsc
